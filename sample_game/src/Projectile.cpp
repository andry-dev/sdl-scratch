#include "Projectile.h"

template <typename APIType>
Projectile<APIType>::Projectile(const glm::vec2& pos, const glm::vec3& direction, float speed, int lifetime, const std::string& texturePath)


template <typename APIType>
